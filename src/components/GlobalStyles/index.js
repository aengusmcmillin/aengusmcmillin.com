import React from "react";
import Typography from "./Typography";
import Reset from "./Reset";

export default () => {
  return (
    <React.Fragment>
      <Reset />
      <Typography />
    </React.Fragment>
  );
};
